interface:
  display_name: "Peer Review"
  short_description: "Run safe multi-model CLI peer reviews"
  default_prompt: "Use $peer-review to infer the review intensity from my request, default to gate if ambiguous, and run independent Claude and Grok Build peer review; keep Codex/GPT and Gemini opt-in unless I explicitly request them."
