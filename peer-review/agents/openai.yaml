interface:
  display_name: "Peer Review"
  short_description: "Manually run a safe external-model peer review"
  default_prompt: "Use $peer-review only when I explicitly request external review. Default to Claude Fable 5 at planning/high with no fallback; add reviewers, gate/critical intensity, higher effort, web access, or a fallback only when I explicitly request them."
